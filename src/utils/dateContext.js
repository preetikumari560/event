import { createContext } from "react";

const dateContext = createContext({
                    date:"default date",
                    month:"default month",
                    year:"default month"

})

export default dateContext